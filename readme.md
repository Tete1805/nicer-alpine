# Nicer alpine base image for docker

- I just need bash => use `./with-bash`
- I need bash, git... => use `./with-utils`
- I need oh-my-bash => use `./with-theme`
- I need oh-my-bash with a custom theme => use `./with-custom-theme`
